export interface Campaign {
    id: string;
    name: string;
    owner: boolean;
}

export interface CampaignPrototype {
    name: string;
}

export interface CampaignMember {
    name: string;
    owner: boolean;
}
